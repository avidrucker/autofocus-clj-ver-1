
(ns autofocus-clj-ver-1.core
  (:gen-class)
  (:require
   ;; [autofocus-clj-ver-1.cli :as cli]
   ;; [autofocus-clj-ver-1.app :as app]
))

(defn -main
  "runs the entire AutoFocus program"
  []
  ;; (cli/run-cli-app app/initial-program-state)
  )


;; (-main)
